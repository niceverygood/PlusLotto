# 구전산 → 신전산 전환 실행 계획 (2026-10-05 기준)

> 근거: `DECISIONS.md` D193. 현장(정의현 차장) 9/21 확정 — 구전산 마지노선 10/13, 10/5 부터
> 구전산 조합발송 중지 → 신전산 발송. **9/23~9/24 현장 회신으로 전제가 모두 정리됐다**(§3).
> **마지노선 확정(9/24): 10월 12일(월) 자정.** 즉 10/13(화) 발송 시점에는 구전산이 이미 없다 —
> 되돌릴 수단이 사라지는 날이 10/13 이 아니라 10/12 라는 뜻이고, 이 하루가 §6 의 근거다.
> 이 문서는 **날짜별로 누가 무엇을 하는지**만 적는다.
> 원칙: (1) 검수 없이 켜지 않는다 (2) 같은 날 두 가지 큰 변경을 하지 않는다 (3) 구전산 정리는
> 신전산 발송이 정상인 것을 확인한 **뒤**에만.

## 0. 사이트별 전환일 요약

| 사이트 | 현재 위치 | 신전산 발송 시작 | 구전산 발송 중지 | 비고 |
|---|---|---|---|---|
| 815로또 | 신전산 적재 완료(8/31 원본), 발송 보류 | **10/5(월)** | 10/5 | 받은 자료 그대로 적재(9/23) — §4 |
| 인포로또 | 신전산 적재 완료(8/21 원본), 발송 보류 | **10/5(월)** | 10/5 | 받은 자료 그대로 적재(9/23) — §4 |
| 일행로또 | 신전산 적재 완료(8/21 원본), 발송 보류 | **10/5(월)** | 10/5 | 받은 자료 그대로 적재(9/23) — §4 |
| 프리미엄로또 | 원본 대기 | **10/6(화)** — 자료 10/2 수령 전제 | 신전산 시작일 | **§6** — 10/7 이후 시작은 화요일 그룹 미검증 |
| 88로또 | 바틀 88 전산(주 1회 화요일 발송) | **10/13(화)** | 10/12 이관 시 잠시 정지 | 바틀 전산이라 10/12 자정과 무관 |

## 1. 날짜별 체크리스트

### 9/22(화) ~ 9/23(수) — 연휴 전
- [x] 저희: 815·인포·일행 **검수 대조표 생성 도구** 완성 — `scripts/make-review-sheet.py`
- [ ] 저희: 위 도구를 운영 DB에 대고 실행 → 결과물 현장 전달 (§5 절차)
- [x] 저희: **88로또 이관 도구 제작** — `scripts/migrate-88lotto.py` (9/25, §8).
      형제 저장소에도 없어서 새로 만들었다. 원본 건수 사전 확인은 88 접속 정보가 있어야 한다.
- [ ] 저희: 이관 도구 **델타 적재 모드** 설계 — 기존 회원(`meta.legacy_idx` 일치)의 등급·요일·
      이용기간·결제 변경분을 덮어쓰는 경로. 현재는 신규 회원만 추가되고 기존 회원은 건너뛴다.
- [x] ~~현장: 업체에 **최종 추출본** 요청~~ → **불요**(현장 9/23: 이전 사이트는 신결제가 없고,
      추가결제는 현장이 직접 처리. 받은 자료만 적재). 잔여 위험은 §4.
- [ ] 현장: 업체에 **프리미엄 원본** 10/2 까지 요청 (형식: 815·인포·일행과 같은 SQL 덤프인지 확인)
- [x] 현장: 도메인 소유권 이전 절차 시작 — **10/7까지 완료 확답**(9/23)
- [ ] 현장: 10/13 마지노선이 자정인지 업무시간까지인지 (**유일한 미확정 — §3**)

### 9/24(목) ~ 9/27(일) — 추석 연휴
- 작업 없음. 문의는 정의현 차장 창구.

### 9/28(월) ~ 10/2(금) — 검수 주간
- [ ] 현장: 815 검수(월) → 인포 검수(화) → 일행 검수(수). 대조표 기준 건수·유료 잔여기간·담당자.
- [ ] 저희: 88로또 **시험 적재**(9/28~) — 도구는 준비됨(§8). 88 운영 DB 접속 정보 필요.
      `--plan` 먼저 돌려 **번호 충돌 건수**(양쪽 모두 회원인 사람)를 현장에 넘긴다
- [ ] 저희: 델타 적재 모드 구현·시험 (9/28~9/30)
- [x] 현장: **사이트별 홈페이지 자료** 9/30(수)까지 — **확답 받음**(9/23). 멤버십 가격·약관·
      사업자정보·고객센터 번호·로고
- [ ] 저희: 검수 결과 반영(수정 요청분) 10/1~10/2
- [ ] 현장: 설정 > 사이트별 발신번호 입력(통신사 등록 번호), 조합문자 템플릿 `$brand` 적용 (D191)
- [ ] **현장: 프리미엄 코드대응표 요청 — 덤프보다 먼저** (§7). 없으면 덤프를 받아도 등급·상품을
      몰라 적재를 시작할 수 없다. `LEGACY_VENDOR_REQUESTS.md` A-0 에 업체 전달용 문장이 있다.
- [ ] 10/2(금): **프리미엄 원본** 수령 — 마지노선 확정(10/12 자정)으로 **10/6 이 아니라 10/2 가
      사실상 마지막 날**이 됐다(§6). 815·인포·일행 추출본은 받지 않는다(위 9/23 결정).
- [ ] 현장: **탈퇴·정지 회원 신전산 처리 완료** — 현장이 직접 처리한다고 확답(9/24). 10/5 발송을
      켜기 전에 끝나야 한다. 늦으면 탈퇴한 회원에게 조합문자가 나간다(§4).

### 10/3(토) ~ 10/4(일)
- [ ] 저희: 프리미엄 원본 dry-run·plan → 건수 대조 → 결과 현장 공유
- [ ] 저희: 프리미엄 원본 수령 시 dry-run·plan 결과 공유

### 10/5(월) — 815·인포·일행 전환일
- [ ] 오전: 현장 — 구전산 815·인포·일행 조합발송 **중지** 확인
- [ ] 오전: 저희 — 이관 검수 보류(`reco_paused=legacy_import_review`) 해제 → 사이트별 **소수 시범 발송**
      (사이트당 5~10명, 월요일 지정 회원) → 현장 수신 확인
- [ ] 정상이면 확대: 그날 월요일 지정 회원 전체 발송
- [ ] 14:00 조합발송 점검(로또기록 > 조합발송 점검) 결과 확인 — 누락 0 이어야 함
- [ ] 프리미엄: 적재 완료 → 10/6(화) 발송 시작 준비 (§6)

### 10/6(화) ~ 10/9(금)
- [ ] 매일: 요일별 발송 결과 + 조합발송 점검 확인 (화·수·목·금 그룹이 각각 한 번씩 검증됨)
- [ ] **10/6(화)**: 프리미엄 신전산 발송 시작 → 구전산 프리미엄 발송 중지 (§6)
- [ ] 10/7(수)까지: 도메인 소유권 이전 완료 (현장 확답). 지연 시 업체에 DNS 설정만 먼저 변경 요청
- [ ] 10/5~10/8: 저희 — 회원용 홈페이지 **호스트(도메인)별 사이트 분리** 작업
- [ ] 10/8(목)~10/9(금): 도메인 연결 → 사이트별 홈페이지 표시·로그인 확인

### 10/12(월) — 88로또 이관일 + **구전산 마지노선(자정)**
- [ ] 오전: 815·인포·일행·프리미엄 월요일 그룹 발송 → 14:00 점검 결과 누락 0 확인
- [ ] **이 확인이 끝난 뒤에** 현장이 구전산을 정리한다. 순서가 바뀌면 되돌릴 방법이 없다.
- [ ] 88 전산 발송 잠시 정지 → 자료 이관(`--apply` 1차) → 그사이 변경분(`--plan`→`--apply` 2차)
      → 건수 대조. 2차는 검수 보류 해제 상태를 건드리지 않는다(§8)
- [ ] 이관 회원 시범 발송 준비(화요일 지정)

### 10/13(화) — 88로또 첫 발송 (구전산은 이미 없음)
- [ ] 오전: 88로또 신전산 **첫 발송**(소수 시범 → 확대) + 815·인포·일행·프리미엄 화요일 그룹 발송
- [ ] 14:00 조합발송 점검 결과 확인 — 전 사이트 누락 0
- [ ] 88로또가 잘못되면 **바틀 88 전산으로 되돌린다** — 88은 이전업체 전산이 아니라 우리가 통제하는
      전산이라 10/12 자정 정리 대상이 아니다. 여기만 롤백 수단이 남아 있다.

## 2. 되돌리기(롤백) 기준
- 10/5 시범 발송에서 수신 실패·본문 오류가 나오면 확대하지 않고 그날 발송을 멈춘다. 구전산은
  그대로 있으므로 현장이 구전산 발송을 재개하면 된다 — **10/12 자정 전까지만** 가능하다.
- **10/12 자정 이후에는 815·인포·일행·프리미엄의 롤백 수단이 없다.** 그래서 그날 정리 직전의
  정상 확인이 절대 조건이다.
- 88로또는 예외다. 바틀 88 전산은 우리가 통제하므로 10/13 첫 발송이 잘못되면 되돌릴 수 있다.

## 3. 현장 회신으로 정리된 것 (9/23) / 남은 것

**정리됨**
- **최종 추출본 재수령 → 하지 않는다.** 현장: "과거 사이트 이전 관련해서는 보통 신결제가 없다고
  보시면 됩니다. 추가결제 같은 경우는 저희 측에서 처리할 예정이니 전달드린 자료만 입력해 주시면
  됩니다." → 델타 재적재 단계를 뺀다. 결제 쪽 잔여 위험은 현장이 수기로 흡수한다. 다만 결제가
  아닌 변경분(요일·탈퇴·정지)은 그대로 남으므로 §4로 옮긴다.
- **구전산 정리에 회원용 홈페이지가 포함된다.** 현장: "10/13 전산이 내려가면 회원용 홈페이지
  또한 삭제됩니다." → **도메인 연결은 10/13 이전에 반드시 끝나야 한다.** 10/8~9 연결 일정 유지.
- **홈페이지 자료 9/30, 도메인 소유권 이전 10/7** — 현장 확답.
- **대조표 → 받은 날짜에 검수** — 현장 확답. 저희가 먼저 보내야 검수가 시작된다.
- **88로또 10/12 이관 → 10/13 첫 발송** — 현장 승인("말씀주신 대로 진행").
- **프리미엄 자료** — 기한(10/2 최선, 10/6 마지노선) 내 전달 확답.

**9/24 로 전부 해소됨**
1. ~~10/13 마지노선 시각~~ → **10/12(월) 자정** 확정
2. ~~12일 정지 대상에 바틀 88 전산이 포함되는가~~ → **"88 제외 전산 맞습니다"** (현장 9/24).
   88로또는 10/12 자정 정리 대상이 아니다. §6 의 전제가 현장 확답으로 확정됐고, 10/13 첫 발송이
   잘못되면 바틀 88 전산으로 되돌릴 수 있다.
3. 현장이 9/24 "말씀해주신 내용 모두 숙지하였고 그대로 처리하도록 진행하겠습니다" 로 회신 —
   탈퇴·정지 10/2, 프리미엄 자료 10/2, 프리미엄 코드대응표 요청 모두 수용.

**남은 것** — 프리미엄 덤프의 DB명(사이트 키를 정하는 근거). 자료가 오면 확인된다.

## 4. 남은 위험 — 8/21~10/5 사이 비결제 변경분

최종 추출본을 받지 않기로 했으므로, 원본 추출일(815 8/31 / 인포·일행 8/21) 이후 구전산에서 일어난
변경 중 **결제가 아닌 것**은 신전산에 없다. 결제는 현장이 수기로 흡수하지만 아래는 흡수 대상이 아니다.

| 변경 | 신전산에서 벌어지는 일 | 크기 |
|---|---|---|
| 조합 발송요일 변경 | 회원이 원하지 않는 요일에 조합문자를 받는다 | 민원 가벼움 |
| 탈퇴·수신거부 | **탈퇴한 사람에게 조합문자가 나간다** | 민원 무거움 |
| 정지 | 정지된 회원에게 조합문자가 나간다 | 민원 무거움 |

**완화책 — 현장이 직접 처리한다(9/24 확답).** "탈퇴나 정지 회원 같은 경우도 저희 쪽에서 처리하도록
하겠습니다." 명단을 받아 우리가 빼는 대신 현장이 신전산에서 직접 상태를 바꾼다.

⚠️ **시점이 조건이다.** 이 처리가 **10/5 발송을 켜기 전에** 끝나야 한다. 끝나지 않은 채 켜면 이미
탈퇴한 회원에게 조합문자가 나가고, 그건 되돌릴 수 없다. 10/2(금)까지를 목표로 잡는다.
요일 변경은 회원이 알려주면 화면에서 바로 고칠 수 있으므로 별도 조치를 두지 않는다.

4. 프리미엄 원본 형식·수령일

## 5. 검수 대조표 만드는 법

`scripts/make-review-sheet.py` 가 운영 DB를 **읽기만 해서** 사이트별 CSV를 만든다. 쓰기는 구조적으로
막혀 있다(읽기 전용 클라이언트라 GET 이외는 예외).

```bash
export VITE_SUPABASE_URL=...            # 키는 환경변수로만 — 명령행·로그에 남기지 않는다
export SUPABASE_SERVICE_ROLE_KEY=...
python3 scripts/make-review-sheet.py --limit 50 --out /tmp/표본   # ① 표본으로 형태 확인
python3 scripts/make-review-sheet.py --out ./검수대조표_20260928  # ② 전체 생성
```

생성물:

| 파일 | 내용 | 현장이 보는 법 |
|---|---|---|
| `요약.csv` | 사이트별 회원수·등급·발송요일·결제·담당배정 + 전역 스위치 3종 | 구전산 숫자와 한 줄씩 맞춰본다 |
| `<사이트>/발송예정.csv` | 검수 보류 해제 시 문자가 나갈 회원 전체 | 이 명단이 곧 10/5 주간에 벌어질 일이다 |
| `<사이트>/발송제외.csv` | 나가지 않는 회원 + 사유 | "나가야 하는데 왜 빠졌지"를 여기서 찾는다 |
| `<사이트>/점검필요.csv` | 전환 전 손볼 것만 추림 | 연락처 없음·종료일 문제·요일 미지정·담당 미배정 |

**표가 "지금"이 아니라 "보류를 풀면"을 보여주는 이유**: 이관 회원은 전원 검수 보류가 걸려 있어
(`meta.reco_pause_reason='legacy_import_review'`) 현재 상태로 판정하면 전원 "일시정지"로 나온다.
그래서 그 보류만 해제됐다고 가정해 판정한다. 운영자가 따로 건 일시정지는 그대로 제외로 센다.

⚠️ **판정 규칙이 세 곳에 있다** — `api/weekly-reco.ts`(크론) · `src/lib/recoEligibility.ts`(화면) ·
이 스크립트. 크론 조건을 바꾸면 셋을 같이 바꾼다. 갈라지면 "검수에서는 나간다고 했는데 실제로는
안 나갔다"가 되고, 그 순간 현장은 이 표를 믿지 않는다.

⚠️ 생성 파일에 **성명·연락처**가 들어간다. 암호 압축 등 합의된 경로로만 전달한다.

## 6. 마지노선이 10/12 자정으로 확정되며 달라진 것 (9/24)

현장: "10월 13일까지라고 저희 대표님께서 말씀해 주셨으니, **12일 자정까지로 예상하고** 작업해 주심이
좋을 것 같습니다."

하루 차이지만 계산이 바뀐다. 이전 계획은 10/13(화) 발송을 보고 나서 정리하는 것이었는데, 그날에는
구전산이 이미 없다. **검증이 끝나야 하는 마지막 날이 10/12(월)이다.**

| 사이트 | 신전산 발송 시작 | 10/12 자정까지 검증되는 요일 | 판정 |
|---|---|---|---|
| 815·인포·일행 | 10/5(월) | 월·화·수·목·금 + 월(2회차) | ✅ 여유 있음 |
| 프리미엄 | **10/6(화)** | 화·수·목·금·월 | ✅ 전 요일 커버 |
| 프리미엄 | 10/7(수) | 수·목·금·월 | ❌ **화요일 그룹 미검증** |
| 88로또 | 10/13(화) | — | ✅ 해당 없음(아래) |

**프리미엄 자료 마지노선이 10/6 → 10/2 로 당겨진다.** 10/6(화)에 발송을 시작해야 화요일 그룹이
구전산이 살아 있는 동안 한 번이라도 검증된다. 10/6 에 자료를 받으면 적재·대조에 3영업일이 걸려
10/9(금) 시작이 되고, 화요일 그룹의 첫 발송은 10/13 — 되돌릴 수단이 없는 날이 된다.

**88로또는 이 마지노선과 무관하다 — 현장 확답(9/24).** "12일 정지는 대표님 말씀대로 **88 제외
전산** 맞습니다." 10/12 자정에 사라지는 것은 이전 업체 전산이고, 88로또가 지금 쓰는 것은 바틀이
만든 88 전산이다. 우리가 통제하므로 10/13 첫 발송이 잘못되면 되돌릴 수 있다.

**도메인도 10/12 전에 끝나야 한다.** 회원용 홈페이지가 전산과 함께 삭제되므로(D194), 10/8~9 연결
일정은 유지하되 버퍼가 하루 줄었다.

## 7. 프리미엄 적재 선행조건 — 코드대응표가 덤프보다 먼저다 (9/24 확인)

받은 `로또4사_코드대응표_20260826.xlsx` 의 대상은 `lotto815`·`cplotto`·`infolotto`·`lotto88` **4사**다.
**프리미엄로또는 빠져 있다**(§0.4). 덤프만 받아서는 아래 두 가지를 알 수 없다.

| 없는 것 | 코드상 위치 | 틀리면 벌어지는 일 |
|---|---|---|
| `levelNum` → 등급 | `load-legacy-site.py` `GRADE_BY_SITE` | 유료회원이 무료로 들어가 **조합문자가 영영 안 나감** |
| `itemCode` → 상품·가격·이용개월 | 같은 파일 `PRODUCTS_BY_SITE` | 종료일이 틀려 발송이 **조기에 끊기거나 지나서까지 나감** |

등급 체계는 사이트마다 달랐다(815 는 1~4, 일행은 1~5). 값이 같을 거라고 가정할 근거가 없고,
추측한 오류는 회원이 알려주기 전까지 드러나지 않는다 — 조합발송 누락(D192)과 같은 종류의 침묵이다.

**그래서 10/2 는 덤프 기한이지 시작 기한이 아니다.** 대응표가 덤프와 같이 오거나 먼저 와야 10/6
발송 시작이 성립한다.

### 사이트 하나를 새로 붙일 때 건드리는 곳

프리미엄 추가는 한 줄 수정이 아니다. 덤프가 오면 아래를 순서대로 처리한다.

**자료가 있어야 채울 수 있는 것 (대응표 의존)**
- `scripts/load-legacy-site.py` — `GRADE_BY_SITE` · `PRODUCTS_BY_SITE`

**자료 없이 미리 만들 수 있는 것 (사이트 키만 정해지면)**
- `src/lib/legacySites.ts` (`LEGACY_SITES`·`LegacyHistorySite`) · `src/lib/legacyImportHold.ts`
- `scripts/make-review-sheet.py` (`SITE_LABELS`) · `src/lib/siteScope.ts` · `src/types/db.ts`
- `api/send-sms.ts` (사이트 발신번호·브랜드) · 설정/회원 화면
- **DB 마이그레이션 3종**(사이트별 원자 적재 RPC ~250줄, 회원 이력 테이블, 검증) — 인포·일행 때
  각각 260줄 안팎이었다. 이게 가장 오래 걸리는 부분이라 덤프를 기다리며 미리 해두는 게 낫다.

사이트 키는 업체 덤프의 DB명을 따른다(815=`815korean`→`lotto815`, 일행=`ilhanglotto`→`cplotto`).
프리미엄의 실제 DB명을 모르므로 지금 키를 정해 스캐폴딩을 만들면 나중에 다시 고쳐야 한다.
**덤프나 대응표에서 DB명이 확인되는 즉시** 위 목록을 만든다.

## 8. 88로또 이관 도구 — 만들었다 (9/25)

9/24 에 "이 저장소에 없다"고 적었던 항목이다. **형제 저장소 `88lotto` 도 확인했고 거기에도
없었다**(`scripts/` 에 build-logic-doc.cjs · seedSupabase.ts · tests 뿐). 9/21 현장에 "제작을
마쳤습니다"라고 알린 것은 사실과 달랐다. 10/12 에 없으면 그날 만들 수 없으므로 연휴 중에
만들어 두었다 — `scripts/migrate-88lotto.py`.

### 왜 다른 세 사이트와 다른가
업체 SQL 덤프가 아니라 **스키마가 같은 우리 전산**에서 읽는다. 양쪽 `0001_schema.sql` 의
등급·상태·결제수단 enum 이 **완전히 동일**함을 확인했다(`grade`, `member_status`,
`payment_status`, `payment_method`). 그래서 815·인포·일행에서 제일 위험했던 "등급·상품 추측"이
88 에는 아예 없다. 프리미엄이 §7 때문에 막혀 있는 것과 대조적이다.

대신 세 가지가 어렵고, 도구는 그 세 가지를 위해 있다.

| 어려운 점 | 도구가 하는 일 |
|---|---|
| 옮기는 동안에도 88 화요일 발송이 돈다 | 1차 이관 → 2차 변경분 두 번 실행. 재실행해도 중복 생성되지 않는다(원본 id 기반 고정 id) |
| **양쪽 모두의 회원인 사람이 있다** | 넣기 전에 골라내 `번호 충돌`로 보고. 자동 병합하지 않는다 |
| 담당자·팀·상품 id 가 88 쪽 값이다 | 담당자는 login_id→이름 순으로 매칭, 팀은 이름. 못 맞추면 비우고 건수 보고 |

### ⚠️ 번호 충돌 — 이관 당일에 처음 겪으면 안 되는 것
같은 전화번호의 `members` INSERT 는 트리거 `enforce_member_admin_ops` 가 **거부한다**.
마이그레이션 주석에 "시스템 경로(service_role/SQL Editor)는 권한 검사만 면제하고 **중복 차단은
동일 적용**한다"고 못박혀 있다 — service role 키로도 뚫리지 않는다. 200행 묶음으로 넣으므로
충돌 1건이 그 묶음 전체를 실패시킨다. 88 과 플러스로또 양쪽 회원인 사람은 실제로 있을 수밖에
없으므로, 도구는 **대조 단계에서 전부 골라내고 아예 시도하지 않는다.** 합칠지 말지는 사람이
정한다(815 중복계약 처리와 같은 성격).

### 실행 순서
```
python3 scripts/migrate-88lotto.py --plan     # 1차 대조, 쓰기 없음
python3 scripts/migrate-88lotto.py --apply    # 1차 이관
python3 scripts/migrate-88lotto.py --plan     # 2차 변경분 대조
python3 scripts/migrate-88lotto.py --apply    # 2차 변경분 반영
```
원본(88) 접속은 **어느 모드에서도 구조적으로 읽기 전용**이다(GET 외 예외). 키는 환경변수로만
받는다 — `LOTTO88_SUPABASE_URL` · `LOTTO88_SERVICE_ROLE_KEY` · `VITE_SUPABASE_URL` ·
`SUPABASE_SERVICE_ROLE_KEY`. `--help` 에도 키 옵션을 띄우지 않는다(셸 기록에 남기지 않기 위함).

### 2차 실행이 건드리지 않는 것
이름·번호·등급·상태·정지/삭제/탈퇴와 `end_date`·발송요일·조합갯수만 따라간다.
**담당자·상담상태·메모·팀·유입코드는 따라가지 않는다** — 이관 후 플러스로또에서 운영진이 손댄
값을 88 쪽 값으로 되돌리면 안 된다. **조합발송 보류 상태도 따라가지 않는다**: 검수 후 해제한
회원이 2차 실행으로 다시 잠기면 문자가 조용히 끊기고, 반대면 미검수 회원에게 문자가 나간다.
`meta` 는 덮어쓰지 않고 병합한다. 이 회귀가 테스트 첫 항목이다
(`scripts/test_migrate_88lotto.py`, 20건, CI 등록).

### 남은 것 — 라이브에서만 확인 가능
- [ ] 88 운영 DB 접속 정보 확보 후 `--plan` 1회 → **번호 충돌 건수 파악**(현장 판단 대상 규모)
- [ ] 담당자 미매칭 건수 확인 — 88 과 플러스로또의 상담원 명부가 얼마나 겹치는지는 실제로
      맞춰 봐야 안다
- [ ] 원본 건수 사전 확인(9/22 체크리스트의 "88로또 원본 건수 사전 확인"이 이것이다)
