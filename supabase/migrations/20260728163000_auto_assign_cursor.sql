-- 자동배분(자동할당) 라운드로빈 커서 (현장 피드백 7/28, 정의현 차장)
-- "이전 분배자에게 재분배되는 상황이 나오고 있습니다" — autoAssign() 이 호출마다 배정 순번을
-- 0번부터 다시 세어, 같은(또는 겹치는) 대상을 나눠서 여러 번 자동배분하면(예: 리셋 직후 그
-- 회원만 다시 자동배분) 배치 내 같은 상대 위치의 회원이 매번 같은 담당자로만 몰리는 버그를
-- 고쳤다(features/members/{api,supa}.ts). 이 컬럼에 마지막으로 배정한 staff_id 를 남겨
-- 다음 호출이 그 다음 사람부터 이어받는다 — 진짜 라운드로빈이 되려면 호출 간 상태가 필요하다.
-- 설정 화면에는 노출되지 않는 내부 운영 상태(사람이 직접 편집할 값 아님).
-- 실행: Supabase SQL Editor. 멱등(여러 번 실행 안전).

alter table site_settings
  add column if not exists auto_assign_cursor text;
